# Windows
Thumbs.db
desktop.ini

# Third-party repo cloned only for reference -- not part of this project
ERR-MapForGoblins-DLL/

# Work in progress mod which is abandoned for now
SummonTorrentEverywhere/
NO LONGER DEVELOPED/
QuestPath/

# Build artifacts (each mod also ignores its own; these are safety nets)
build/
out/
.vs/
*.user
CMakeSettings.json

# Mod runtime logs
logs/

# Internal notes / scratch markdown -- keep only README.md files in the repo
*.md
!README.md

# python venv
venv/
